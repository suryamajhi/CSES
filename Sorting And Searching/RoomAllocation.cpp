#include<bits/stdc++.h>
using namespace std;

const int N = 0;

int main(){
	int n;
	cin >> n;
	vector<pair<int, int>> c(n);
	for(int i = 0; i < n; i++) {
		int x, y;
		cin >> x >> y;
		c[i] = {x, y};
	}
	
}